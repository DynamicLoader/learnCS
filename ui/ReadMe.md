## UI Files

The project is using Qt QML Engine, and this directory contains GUI design, interface logic and resource files.

Note that all `.qml` file in this directory will be compiled and add to the application; while all `.js` file in directory `handler/` will be done too.

